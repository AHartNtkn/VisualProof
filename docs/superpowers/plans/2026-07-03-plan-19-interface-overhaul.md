# Plan 19: The interface overhaul — interaction design first

**USER DIRECTION (2026-07-03):** the overhaul is NOT just visuals/layout — "it also means things related to interaction. For example, applying different rules, connecting wires… right now almost none of these feel very good to actually interact with." Process correction from the render rounds: "you weren't very thorough on what does and doesn't get demoed… started asking about things that relied on previous things that weren't demoed. Some planning for thoroughness would go a long way." Therefore: the COMPLETE component inventory below is the contract; every item is assigned to a demo round; no round consults the user on anything whose prerequisites have not themselves been demoed and decided. The user is the design eye (collaboration protocol in rendering-laws memory): each round presents MULTIPLE genuinely different mechanisms as WORKING demos against the real engine, never sketches.

## The complete inventory

### A. Direct manipulation on the sheet (the foundation — everything else composes with it)
- A1 Selection: what a click/tap selects (node, wire, region); multi-select; deselect; marquee/lasso; visual selection state; what HOVER communicates (selectable? draggable? valid target?)
- A2 Moving: body drag, region drag (exists — feel re-judged in context), pinned-vs-released semantics (arrangement persists, position doesn't — standing design question)
- A3 Zoom (fixed background — settled law) + any focus/navigate affordance (double-click to zoom-to-region?)
- A4 Undo/redo: depth, visibility, keyboard; redo currently absent
- A5 Wire connection (join): today click-wire, click-wire, press button — candidate mechanisms: drag from line-end to line-end; drag port-to-port; magnetic snap on release
- A6 Wire severing: today a rule buried in menus — candidate: cut gesture (stroke across a wire), select+key
- A7 Node creation (term): today a λ-text box + Add — candidates: text box retained but placed well; palette drag-out; on-canvas radial create; structured term builder
- A8 Node creation (relation refs): today chooser buttons — candidates: palette of known relations; drag from Library entry onto the sheet
- A9 Cut/bubble creation: today select+wrap button — the Peirce-native candidate: DRAW the circle around content (lasso = cut); modifier for bubble+arity
- A10 Delete: select+button — candidates: key, drag-to-void, gesture

### B. Rule application (the proving interactions — depends on A's selection/gesture vocabulary)
- B1 Rule discovery/browsing: how the user learns what applies HERE (today: text-button menu appears after selection). Candidates: contextual menu (radial/linear) at the selection; always-visible rule palette that lights up by applicability; hover-preview of each rule's RESULT (ghost diagram) before commit
- B2 Rules with a target (iteration into a region; insertion): pick-target second phase vs drag-the-selection-into-the-region as a gesture
- B3 Rules with arguments (theorem citation): the two-phase wire picking — inference where possible (fold already infers); citing = choose theorem, args inferred when unambiguous, picked otherwise
- B4 Conversion (βη): term input + fuel on a node — candidates: normalize button + step-wise reduction preview; inline term editing
- B5 Polarity awareness: making positive/negative visible where it gates rules (region shading exists; is it enough?)
- B6 Structural rules (double cut, vacuous, erasure, severing): discoverability + gesture mapping per A
- B7 Comprehension instantiate/abstract: the most parameter-heavy flows — staged wizards vs in-place picking
- B8 Refusal presentation: kernel messages are the UX copy (law) — WHERE they land (status line vs anchored at the refusing thing) and how they persist

### C. The proof model surface (depends on B)
- C1 Goal setting: LHS/RHS snapshots — today two buttons over the live sheet; candidates: explicit goal panel; drag current sheet into a goal slot; goal-first workflow framing
- C2 Session/meet presentation: forward vs backward sides, the meet check, side switching — today a toggle + status text; candidates: two-lane view (companion is the seed), explicit meet indicator
- C3 Derivation history: the recorded steps — today invisible except Undo; candidates: step list/timeline with jump (replay machinery already supports it)
- C4 Assemble/check/name: theorem naming input + assemble — placement and flow
- C5 Replay: stepping UI (buttons/keys exist) — scrubber/timeline; step labels; entering/leaving replay
- C6 The companion (PiP/split): default state, placement, interaction with modes (machinery shipped in plan 17)

### D. Chrome assembly (depends on A–C decisions — LAST, not first)
- D1 Overall layout: where library/actions/status/inputs live; what is permanent vs contextual
- D2 Mode identity: EDIT/PROVE/REPLAY made visually unmistakable
- D3 The Library panel: files, load state, session group, per-theorem actions
- D4 Status/messaging surface (with B8)
- D5 Theme system (LIGHT/DARK settled as looks; chrome must join them)
- D6 Keyboard map + power-user layer
- D7 Text inputs that remain (λ-terms, names, fuel): placement, validation feedback

### E. Motion & feedback polish (throughout, judged at the end)
- E1 Rule-application transitions (today: rebuild + resettle; candidate: staged morphs)
- E2 Physics feel parameters in final context
- E3 Hover/selection animation timing

## Demo rounds (dependency-ordered; each round = 3–4 working variants per question, real engine, user judges)

- **Round 1 — A1 selection + hover vocabulary.** Everything downstream reads through it. Variants differ in what hover shows and how selection accumulates/clears.
- **Round 2 — A5/A6/A9 construction gestures** (join, sever, draw-a-cut) + A7/A8 creation. Uses Round 1's selection.
- **Round 3 — B1/B2 rule application model** (discovery + targeting), on Round 1–2 vocabulary. The biggest single feel item.
- **Round 4 — B3/B4/B7 parameterized flows** + B8 refusals.
- **Round 5 — C1–C5 proof model surface** (goals, sides, history, replay).
- **Round 6 — D assembly**: full chrome variants integrating all prior decisions; C6 placement; D2 mode identity.
- **Round 7 — E polish pass** on the winner.

Rule of the process: a round's demos may ONLY depend on machinery already decided in earlier rounds (or shipped pre-overhaul). If a variant needs an undecided dependency, it moves to the round after that dependency. The inventory above is the checklist — every item gets a ⟨round, decided-in, verdict⟩ record as the rounds complete.

- [x] Round 1 demos built and judged. **VERDICT (user, 2026-07-03): A1 = variant D's brush** — drag paints items in/out of the selection, plain click toggles one, empty click clears (subsumes B's shift-click toggle, which the user liked); B judged strictly better than the current model, D a step above B. **Required fix adopted:** hover must stay visible on already-selected items (B's darkening vanished there; on wires there was NO signal at all). Realized as `ui-lab/round1-e.ts`: hover on unselected = blue tint/stroke (the would-be catch); hover on selected = the amber darkens (dark-amber #92400e tint on nodes/regions, wider dark-amber restroke on wires); readout appends "— selected". Verified by screenshot on both the selected-wire and selected-node cases.
- [x] Round 2 demos built and judged. **VERDICTS (user, 2026-07-04):** A (verbs-on-selection) incremental — but context-aware menus are a good idea (feed into Round 3). B (direct gestures) directionally correct: drag-join and slash-sever KEEP; lasso too tedious for large diagrams — replace with select + Space = cut, Shift+Space = SO bubble; double-click-for-term is a wasted double-click. C: keep BOTH slash and double-click sever behind an options toggle (slash default); radial spawn menu = right idea, wrong implementation — cut does not belong in it, the trigger collided with selection, and it cannot scale to thousands of relations (**the spawn browser is its own design round, queued below**). Mechanical rulings, all realized in `round2-d` (the composite): N-ary join (J joins all selected lines); sever immediate (no pick-again phase); selecting a cut + its contents wraps the subtree once (absorbHits); Delete works across regions and DISSOLVES selected boundaries (contents propagate up, selected contents die); drag a selected node to MOVE it between regions (reparent with wire-scope correction — wholly-owned wires travel, shared wires keep scope when valid, else tightest common ancestor). Physics discovery: regions dodge an approaching dragged node (sibling repulsion), so placement drags FREEZE settling until release. App-level findings to port at integration: (1) `edit.ts wrap()` leaves wholly-enclosed wires scoped OUTSIDE the new cut — semantic mismatch with what drawing a circle around them says; (2) `hitTest` claims a region's whole disc, so "empty space inside a cut" never exists for click/dblclick triggers — any spawn-menu trigger must not depend on hitting nothing.
- [x] Round 2b — the spawn browser. **VERDICT (user, 2026-07-04): C — the contextual cascade — wins outright** ("definitely the best option; I don't see a reason to use A or B with this"). A (palette) functional but bad for exploration — can't browse; possibly a component of a larger library interface later. B (panel) clean, strictly better than A, but subsumed by C. The spawn surface going forward: still right-click → cascade at the point (search + λ-term + recents + namespace submenus), spawning where clicked. Built: three surfaces inside the full composite, browsing a synthetic 140-relation/11-namespace library (`ui-lab/library.ts`; composite factored to `ui-lab/composite.ts`). **A — command palette** (`/` at cursor: ranked substring search, ↑↓+Enter, `\…` = λ-term, empty query = recents; spawns at invocation point — key trigger, so region discs can't eat it). **B — library panel** (folding sidebar: search + namespace groups + recents; rows drag out; drop point = region). **C — contextual cascade** (STILL right-click opens menu at point — right-drag still slashes, selection untouched; search row, λ-term, recents, namespace submenus on hover; spawns at the clicked point, inside cuts included). All 13 gesture checks green. Observed for integration: long namespaced defIds truncate on the node disc — label display needs a rule (e.g. name sans namespace).
- [x] Round 3 built and judged. **VERDICTS (user, 2026-07-04/05):** consolidation rejected — the few moves deserve DEDICATED mechanics, not one menu (A "functional, not good"; B's palette "emphasizes how few options there are" and still demands prior knowledge). C directionally correct: **drag-to-iterate stays**. Previews inverted into a **memory box** (show what Ctrl+Z restores, labeled with the move — preview = make the move, then undo). Streamlining rulings, all realized in `round3-d`: **Delete = contextual deletion** (double-cut elim / vacuous dissolve / positive erasure / deiteration resolved by selection, absorb-normalized — selecting BOTH cuts of a double cut works; "arbitrary secrets" to access rules must not exist); **W = double-cut wrap, Shift+W = bubble wrap** (construction composite moved off Space too; Space freed). Hint strip states the bindings' meaning per selection. Confirmed: "The implementation works how I'd imagine." Also landed from this round's bug report: **USER LAW — a line's outermost point is a body homed at the wire's scope** (loose ends are their own nodes; ∀-shape wires grow a dangling ∃ branch at the scope, junctions home at the port dca, never contort; ui-lab/forall.html is the visual reference; tests/view/stub-scope.test.ts pins it). Open interpretation the user may refine: no dangle when scope == dca. PROVE labs on a polarity-rich showcase (`ui-lab/prove.ts`): discovery = the app's own `applicableActions` (mirrored gates), commits call kernel appliers, refusals verbatim; shared iterate target phase (legal regions glow, click commits, Esc cancels); fuel = the shell's 64 default. **A — contextual rule menu** (still right-click on a selection lists its legal moves; parameterized kinds dimmed "(round 4)"). **B — lit rule palette** (full vocabulary always visible; selection lights applicability; hovering a dim rule teaches its gate; polarity line names the selection's region sign). **C — ghost previews** (chips by the selection; hover renders the RESULT diagram in an inset via the real pipeline; drag-the-selection-into-a-region = direct iterate with per-target preview). 16/16 machine checks incl. real deiteration, double-cut elim, gate mirroring (no Erase in negative), and a kernel refusal surfaced. Lab fix with app parity: engine swap now settleSteps once (paint requires it — same discipline as the shell).
- [x] Round 4 built and judged. **VERDICTS (user, 2026-07-05):** A/B's citation flow BROKEN in practice — hand-acting as the pattern matcher has a "narrow corridor" (root cause: the applier needs the theorem's boundary wires TOUCHING the selection, not inside it, so the natural brush-everything gesture yielded "0 attachment wires"); listing every theorem in the menu is "insane" — a LIBRARY problem (2b's answer) with context filtering: never show a theorem that can't apply, never show one not containing the selected node; edge order should be forced/inferred (manual pick optional at most). **C is "the obviously correct thing"; tab-cycle the correct ambiguity answer** — refined: selection must disambiguate (occurrences containing it only). On-diagram beats side panels ("not forcing the user to look away… good design principle"), but keep A's head-normal options. βη: quick normalize on DOUBLE-CLICK + head-normal + custom target all essential; presentation should ANIMATE a→a′←b (play the target's reduction backwards; speed control, off-switch) — queued for Round 7 motion. INSTANTIATE conceptually wrong twice: (1) must not require a NAME — an on-the-side diagram-construction box (the EDIT interface, miniaturized) should supply anonymous comprehensions (QUEUED as own design item); (2) named substitution must stay FOLDED — never auto-expand (fixed: `foldedComp` splices the ref, kernel accepts; PORT TO APP: shell passes the body today). NEW GLOBAL RULING: **Ctrl+drag = physics-only handle** (no proof/diagram meaning) — shipped in the shared brush. All realized in `round4-d` (14/14 checks incl. the exact failing gesture, selection-disambiguated apply, folded instantiate, ctrl-drag inertness). Parameterized flows against the REAL verified Frege theory (`ui-lab/prove4.ts`: verifyTheory(buildFregeTheory)); showcase = two succNat-lhs occurrences + β-redex + negative guard bubble; entry = the decided still-right-click menu (parameterized moves + theorem list; unfold commits immediately). **A — staged panel** (side panel walks parameters: cite args as ordered chip list filled by clicking wires; convert prefills the node's term + normal-form tactic buttons; refusals render IN the panel). **B — in-place picking** (numbered badges on argument wires drawn on the diagram, Enter/Esc; convert = inline prompt at the node; refusals = status line). **C — infer-first** (citing runs the occurrence matcher on the theorem's from-side: unique → applies instantly with derived sel+args; several → highlighted cycling (Tab/click, Enter); convert = weak-head→head→custom fallback chain; fold/instantiate ask only the relation; refusals = anchored bubble at the selection, auto-fade). Closed theorems cite as empty-selection insertions everywhere. 10/10 machine checks incl. kernel-applied succNat via badges AND via inference (occurrence 2/2), plus real refusal copy in all three placements.
- [x] Round 5 judged (2026-07-05/06). **VERDICTS:** D (derive-then-declare) is "a version of what I described" — confirmed as the standard flow. **Dual-front ruling:** when approaching from two directions, BOTH sides interactive in a split screen, with declaration checking graph isomorphism to complete — recorded as the integration design (two live viewports = round-6 chrome work; the meet check already IS canonical-form isomorphism). **History:** clunky but click-to-peek is good; SHIPPED click-to-REWIND (chip click jumps the track; hover peeks); a dedicated history-display design pass QUEUED. **THE BIG RULING — backward reasoning is the SAME calculus with polarity flipped**, and the implementation must be SHARED, never mirrored: realized as `applicableActions(..., backward)` (one boolean; erase/insert gates swap, citation direction = sign XOR orientation), `applyStep(..., orientation)` threading to the three polarity-gated appliers (gate stays inside the applier — no gate-free entry), and `applyBackward(session, step: ProofStep)` where the only backward-specific code is `inverseStep` diff bookkeeping (guarded by the reproduction assertion + declare-time replay). The five hand-mirrored `un*` BackwardActions were DELETED (shell + tests migrated); backward tracks now take the full vocabulary (Delete/W/drag/dbl-click/menu) everywhere. Invertible set: erasure, insertion, double-cut, vacuous, iteration/deiteration (justifier search via the matcher), conversion, theorem, fold/unfold; instantiate is forward-only until its abstract-inverse is built (offered nowhere backward). vitest 897/897 (+6 flipped-vocabulary kernel tests), e2e 8/8, lab smokes green. Prior D framing below stands: goal-first (draw lhs, draw rhs, connect) "would essentially never be used — redundant"; the standard flow is **pick a direction, derive, DECLARE** — the other end falls out of the proof for free. The codebase's own authoring (DerivationCursor in frege.ts) already works this way; checkTheorem needs no session. Realized in `round5-d` (mkTrackLab): F/B starts a forward/backward track from the sheet, D declares (origin ⟹ here) or (here ⟹ origin) — backward declaration uses the composedTail (exact replay steps); kernel-checked, adopted, repeat-declare stacks lemmas; single-ended timeline peeks states. 7/7 checks (incl. zero-step identity declare and the context filter correctly omitting a just-declared theorem where its premise no longer occurs). The two-ended meet (5a/5b/5c presentations) demoted to the special case for statements fixed a priori — an optional "second front", not the entry ritual. The verdict move layer was factored to STEP-EMITTING form (`ui-lab/verdict.ts`: rounds 3–4 decisions producing ProofSteps through a sink) and wired to a REAL ProofSession (`ui-lab/session5.ts`: startSession/applyForward/applyBackward/meet/assembleTheorem+adopt; goal = succNat's own statement; backward side takes un-citations via the same infer-first matcher over theorem to-sides). Presentation variants: **A — two-lane split** (other side always visible, click to swap, meet banner), **B — PiP target** (corner inset "meeting: X", S swaps — plan-17 companion session-refined), **C — proof timeline** (bottom strip grows from both ends toward the gap; chips peek historic states — the memory box generalized; the gap chip assembles on meet and the strip becomes a replay scrubber with ←/→). 12/12 checks: forward cite meets; backward un-cite meets from the other end; assembly re-checked by replay and adopted (immediately citable); timeline peek + replay scrub. Known 5a roughness: the main view fits to full width under the overlay lane (real split cameras are integration work).
- [ ] QUEUED (back of queue, user ruling 2026-07-06): design comprehensionInstantiate's backward inverse (an abstraction-step construction — occurrences + wrap derivation from the instantiation diff) so instantiation joins the invertible set.
- [x] Round 6 judged. **VERDICT (user): A — minimal sheet — wins outright**; B's and C's additions "objectively redundant and less convenient than what A already provides." The history display remains "clunky and poorly designed" (second flag) → the queued history design pass runs NEXT (round 6h) on A's chrome. Built: The MODE MACHINE (`ui-lab/chrome.ts`): one canvas hosts both decided vocabularies — installBrush/installComposite/installVerdictMoves gained `active` gates; EDIT right-click = the spawn cascade over REAL relations (`ui-lab/spawn.ts`, the 2b-C verdict); F/B start a derive-then-declare track from the current sheet, D declares, E exits back to EDIT keeping the sheet; ONE ProofContext persists across tracks (declared theorems stay citable). Variants: **A — minimal sheet** (mode pill only; timeline while proving; '?' keyboard map), **B — studio** (toolbar with mode badge + lifecycle buttons; library panel — relations drag out in EDIT, theorems click-to-cite with live applicability in PROVE; docked timeline), **C — summon** ('/' palette unifying actions + spawns + applicable citations, searched together). All boot from an EMPTY sheet; 20/20 lifecycle checks (spawn→construct→F→cite→declare→E→construct). Also fixed en route, test-first: **zero-endpoint wires (a bare ∃, legally produced by erasing a node out from under its wire) crashed mkEngine** — now a scope-homed degree-0 body whose dot IS the wire (stub-scope.test.ts pins it; suite 898).
- [x] Round 6h judged. **VERDICTS (user):** A's bottom placement wrong (covers the diagram, splits eyesight — horizontal belongs at top, or go vertical); film idea conceptually right and possibly better than text for large proofs, but whole-diagram miniatures are unreadable — **zoom to the affected part instead**. B = vertical A, compact, not obviously worse; its half-height column styling was wrong. **C closest to the target: scrubbing = rapid undo AND redo, strictly superior to click-to-undo-no-redo** — thin bar earns its bottom placement. Realized in `round6h-d`: TrackLab is now CURSOR-based (the future is retained across rewinds; redo = Ctrl+Shift+Z; a new move from an earlier point discards it — the linear undo/redo ruling, resolving the branch question), the scrubber drags the real cursor, future ticks render dashed, hover pops the step's change ZOOMED (`zoomThumb`: fresh nodes + wire bodies focus the preview; pure removals fall back to whole-diagram), declare works AT the cursor (steps up to it). 8/8 checks. **BUG (user report) fixed test-first:** `applyRelFold` minted its ref id against the POST-removal set — resurrecting a just-deleted id (their unfold-unfold-fold-fold sequence) and breaking diff-based inverses; now mints against the full pre-removal set (the applyTheorem discipline); reldef-fresh.test.ts pins the exact sequence. Suite 900/900.
- [x] (built entry, superseded) — on the winning minimal chrome, with humanized step labels (`stepLabel` in session5.ts; rule identifiers are jargon). **A — filmstrip** (a cached thumbnail per state — `ui-lab/history.ts` — captioned with the move; click rewinds; horizontal scroll). **B — ledger** (numbered step column reading like a proof script; hover = floating thumbnail; click rewinds; folds). **C — time scrubber** (a slim bar; dragging time-travels the MAIN VIEW through past states; release = dwell with input held; Enter rewinds there, Esc returns to now). 10/10 checks. OPEN design question surfaced by all three: rewinding then moving DISCARDS the future (linear-overwrite) — an undo-tree/branch model is the alternative; needs a ruling.
- [x] BACKWARD REDESIGN (user ruling 2026-07-06: "completely rethink how you are doing backwards reasoning… potentially a full redesign"): after a third inverse-reconstruction bug (backward deiteration of bound predicates — occurrence search blind to binder-stubbed patterns), the diff-based inverseStep architecture was ROOT-CAUSED as a parallel implementation and DELETED WHOLE. New form: `Theorem.backSteps` (backward actions as given, replayed from the rhs flip-gated) + `checkTheorem` dual replay meeting at canonical form; `applyBackward` = execute + record (~10 lines); orientation param completed across ALL polarity-gated appliers (wireSever/wireJoin/comprehension instantiate+abstract joined erasure/insertion/theorem — full audit, everything else documented direction-free), so the entire rule set is backward-legal and the queued instantiate-inverse item is RESOLVED by architecture. Serialization carries backSteps (classic files unchanged). Deleted: inverseStep, composedTail/tailHistory, session composeProofs, reproduction assertion, the invertible-set boundary. The user's scenario passes with zero special-case code; tamper tests pin that forged backSteps cannot certify. vitest 903/903, e2e 8/8, all lab smokes green.
- [x] Round 7 built (2026-07-06), judged through four iterations — MOTION on the winner stack (`ui-lab/round7.ts`; the 6h-d scrubber factored to `history.ts installScrubber`; chrome gained an interceptStep hook). All layers view-only (layer law) and individually toggleable: **βη animation** (the user's a → a′ ← b design: the node's drawn anatomy steps through the certificate's reduction states — leftSteps forward, rightSteps reversed — via applyStepAt; anchors stay put; the real step commits after playback; speed 0.25–3×; input held during play); **transition ghosts** (dying bodies fade out at their last position via the new lab.onMutate hook; born bodies pulse a ring); **hover ease** (tint ramps in ~120ms; motionPrefs in the shared brush). 7/7 checks incl. deferred-commit and played-2-steps. **User verdicts (2026-07-05/06): iteration 1 "ugly — no smooth transition, wires disconnect"; iteration 2 (anchor lerp + dissolve overlay) STILL REJECTED: "fading… still hard-snaps… wires still disconnect… I want to see attempts at continuous shape morphing."** Iteration 3 (2026-07-06): TRUE GEOMETRY INTERPOLATION — `src/view/morph.ts mkGeomMorph(from, to)` builds one interpolated NodeGeometry per frame: port rails pair BY PORT NAME (the anchor is by construction the rail's outer tip, so per-frame anchors derived from the interpolated rails keep wire endpoints ON the drawn rail with zero error — pinned d=0 by probe); other radials and arcs pair greedily by polar proximity (same-kind preferred); the unpaired collapse to / grow from zero extent in place; exit line/arc lerp; no crossfade, no swap, no overlay. Dying ports ride their retracting rail inward. 6 unit tests (endpoint fidelity, anchors-on-rail at every p, dying-port retraction, ε-continuity) + 6/6 morph probe + 8/8 r7 probe. ALSO SHIPPED from the same user report: spawn cascade dismissal (global Escape + pointerdown-outside, `spawn.ts`) and **auto-deletion of orphaned wires** (USER ruling "wires that connect to nothing should just be auto deleted"): EDIT deleteHits takes fully-covered wires along; PROVE erasure sel extends with same-region orphans (deiteration deliberately NOT extended — a rider wire the master occurrence lacks would refuse the pattern match); 10/10 probe. **User verdict on iteration 3 (2026-07-06): "much, much better" — remaining defect: radial edges visibly detach from arcs mid-morph; RULING: attempt connection maintenance — "things shrink while they are still connected and disappear before they have a chance to [dis]connect"; PIN the current implementation.** Iteration 4: GRID-SPACE MORPH (`mkGridMorph`) — bendGrid is a pure image of integer grid coordinates through theta(col)/radius(row) (now exposed as `bendMaps`), so incidence IS a shared coordinate and linear interpolation of paired grid coordinates + the frame preserves every surviving junction exactly, no snapping. Three-phase clock (equal thirds): dying pieces retract INTO their host while the grid holds still (a collapsing bar sweeps its stems up via col-clamp; a dying pierce dives into the rim with its anchor); the paired skeleton morphs; born pieces grow OUT of their hosts on the settled target grid. Pairing: rails/drops by port NAME, lam/app bars and var/output stems by least on-screen travel with HOST-COHERENT pairs ranked lexicographically first (an incoherent pair detaches mid-B — the exact defect). The invariant "every var/port radial with positive extent hangs on an arc at every p" is pinned at 61 sample points over 4 reduction chains (caught two real bugs: port drops pairing across non-paired hosts; plural output stems dropped by a find()). mkGeomMorph stays PINNED behind the ⚙ toggle 'connected morph (off = pinned v1)'. 12 morph unit tests; probes green. **User verdict (accepted): "This is an improvement. I think we can keep this." — the grid morph is the round-7 motion answer; the pin stands per the user's explicit instruction.** REMAINING for this round: the integration plan (laws-as-tests) — written after the motion verdict so it incorporates every ruling.
- [x] Integration plan filed: `2026-07-04-plan-20-interaction-integration.md` — 26 laws-as-tests, 6 tasks, lab modules as reference implementations, teardown + adversarial review. Queued within it: both-interactive split fronts, anonymous-comprehension construction box, dropped-node stickiness, history-display refinements. (The instantiate backward inverse was RESOLVED by the dual-replay redesign; the conversion animation SHIPPED in round 7.)
