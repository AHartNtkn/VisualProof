# SDD ledger — plan: docs/superpowers/plans/2026-07-30-primitive-wire-quantifier-lean-completion.md

Board T001: complete — preflight verified isolated worktree e42b803, no prior task completion, dependencies present, baseline 113/113 files and 684/684 tests passing outside sandbox, Lake 96/96 jobs, formal:size passing; corrected Task 3 identity-substitution dependency to Task 4.

Board T002 / plan Task 1: complete at 091cc84 — exact 34-tag Lean inventory, flipped-polarity erasure with backward-negative acceptance, direct `negative_splice_sound`, named structural audit, RED evidence, Lake 97/97 jobs, formal:size, and architecture Vitest 2/2 all reviewed and passing. Activated T003 / plan Task 2.

Board T003 / plan Task 2: complete at 1c2b6ba — full-model monolithic relation sever/join specification completed and isolated; one checker-constructed `Model.reify` witness; full build and size gate reviewed and passing.

Board T004 / plan Task 3: complete at 648f494 — generic signature-indexed wire partition/merge over `PreModel`; iota-only modules and facade deleted; full build, size gate, and obsolete-authority audit reviewed and passing.

Board T005 / plan Task 4: complete at 5575690 — eager identity normalization generalized; substitution derived from iteration, scoped sever, and normalization; all IdentityRetarget authority deleted; full build and audits reviewed and passing.

Board T006 / plan Task 5: complete after initial 3e3595f and boundary correction c2ce4bc — exhaustive applied-site receipts and shared-witness theorem retained; production constructor internalized; full build, size gate, and public-API audit reviewed and passing.

Board T007 / plan Task 6: active — architecture review selected one checker-derived simultaneous multi-site factorization/common-core certificate. Sequential one-site semantic steps and caller-supplied whole-diagram equations are rejected. Task scope was expanded narrowly to the shared factorization module plus `Witness.lean` and `WitnessFixtures.lean` so the obsolete internal semantic-equation constructor can be replaced rather than preserved as a parallel authority.

Board T016 / plan Task 6: active — the endpoint-free wire-deletion compiler now retains its existing local implication plus the compiler-proved local equivalence, reconstructs the removed binder value in the reverse direction, and exposes scope/root denotation equivalences. Full `lake build` (111 jobs) and `formal:size` pass. The next required receipt strengthening is the coherence equation between the folded scope-visible renaming and site-outer renaming; without it the universal witness cannot be closed through the acted binder.

Board T016 / plan Task 6: active — the applied-site erasure fold now has one coherent authority: each step and the folded trace retain the scope-visible projection, site-outer projection, their extension equation, the universal acted-wire body law, and the constructor-preserving outer zipper. The former independent `UniversalScopeTransport` identity/composition fold and dead one-way erasure normalization were deleted; the public scope receipt is only a projection of the coherent trace. `lake build` (111 jobs), `formal:size`, focused content fixtures, `git diff --check`, and the no-`sorry` audit pass. Next: close the endpoint-free acted binder and expose ends delete/spawn denotation.

Board T016 / plan Task 6: active — commit 6e974b6 closes the all-end pair over full `Model`: the checker verifies canonical endpoint-free wire deletion, the folded erasure trace reassigns the unused relation binder to the universal relation, and public delete/spawn receipts expose theorem-form and method-form soundness in the checked polarity. Focused content fixtures and `formal:size` pass; the 3,000-line gate forced the endpoint theorem into the dedicated `ContentEndsSemantics.lean` owner.

Board T016 / plan Task 6: active — commit 9d122fe proves the remaining model-side witness algebra without the monolithic relation rule. Cut wrap/absorb use one shared full-model complement relation in both directions; parallel split/fuse use pointwise intersection for elimination and diagonal copying for introduction. Focused fixtures and `formal:size` pass. Next: construct the checker-owned simultaneous concrete factorization connecting these logical witnesses to wrap/split site ledgers, then expose the four public equivalence receipts.

Board T016 / plan Task 6: complete at 4f6f4bd — executable paired-context, suffix-alignment, and environment-reconstruction certificates close the full source and target binder blocks; cut and parallel instantiate complement, diagonal, and intersection witnesses over full `Model`; all six public receipts expose `.sound`; mixed-site, empty-site, polarity, refusal, ordered-argument, and inverse-isomorphism fixtures pass. Full `lake build` (118 jobs), focused content fixtures, `formal:size`, `git diff --check`, and the no-`sorry`/`admit` audit pass. Activated T008 / plan Task 7.

Board T008 / plan Task 7: complete at f8fa460 — executable all-end argument rewrites and full-model receipts cover arity shift/unshift, permutation, duplicate/contract, and merged drop/extend semantics. Scope-normalized cylindrification removes rewritten relation heads into explicit semantic slots and checks the complete remaining binder blocks, including fresh wires at the acted scope and in nested regions. Focused fixtures, full `lake build` (127 jobs), `formal:size`, `git diff --check`, and the no-`sorry`/`admit`/`axiom` audit pass. Activated T009 / plan Task 8.

Board T009 / plan Task 9 raw-adequacy reset: active — `/tmp/vpa-task9-raw-adequacy-foundation-20260802.md` is controlling. Compiler adequacy directly relates the primitive program's raw target to join `plainFinal` or the sever raw target by `ConcreteIso`, with exact ordered boundary transport. Identity normalization is outside this theorem boundary. The orphaned priority chain is removed; compiler construction remains active.

Board T009 / plan Task 9 raw-adequacy reset: complete at `7df213a` — corrected plan and goal controls to direct raw join/sever adequacy; deleted the 11-module orphaned priority/confluence chain; standalone Task 4 normalization unchanged; serialized full Lake build and hygiene scans pass; task-scoped spec and quality review approved with no findings. Task 9 compiler construction remains active.

Board T009 / Task 9 size checkpoint: complete at `f0631a5` — argument authority split into acyclic submodules; full `lake build` passes (164 jobs); all nine argument files are below 3000 lines; `formal:size` now fails only `Rule/MonolithicWireQuantifier.lean` at 5205 lines. Compiler integration must preserve these module boundaries.

Board T009 / Task 9 sever evidence: retained ordinary raw reconstruction evidence shows compiler adequacy must use `ConcreteIso` rather than carrier-value preservation. The raw-adequacy foundation is controlling; the task remains active. Historical investigation: `task-9a-dense-reconstruction-report.md`.

Board T009 / Task 9 raw-atlas preflight: checkpoint complete at `f49ebc3` — raw-origin-only join facts were isolated into a focused module, the concrete join owner returned below 3,000 lines, and a repeated-alias fixture checks every raw row plus exact ordered endpoints against independent expected tables. Full Lake, focused builds, `formal:size`, hygiene scans, and fresh spec/quality review pass. Universal `Conformance` remains active: the next slice must add prefix-neutral occurrence-region landings before node/request and wire/endpoint folds.

Board T009 / Task 9 raw-region trace: total-landing checkpoint complete at `db9f7e5` with review corrections at `ec7d667` — a construction-owned Prop-valued prefix relation covers retained and occurrence regions, proves every origin lands through the ordered splice trace and terminal deletion, and remains independent of atlas readback, compiler search, and normalization. Focused/full builds and size/hygiene gates pass; fresh review approved after removing a premature atlas import and correcting an overclaim. Functionality, atlas identification, and universal occurrence-region rows remain active.

Board T009 / Task 9 raw-region uniqueness: functionality checkpoint began at `667743c` and its review-required ownership replacement completed at `b474d07` — the join owner now builds one dependent Type-valued `RelationJoinConstructionTrace` containing every exact nil/snoc receipt; the bare arbitrary step spine and stored semantic-trace authority were deleted, with the Prop view derived from the construction trace. Region landings recurse directly on that trace and prove totality, forward functionality, inverse uniqueness, and terminal counterparts. Full Lake, focused reconstruction surfaces, size/hygiene scans, and fresh re-review pass. Exact occurrence-region rows and atlas identification remain active.

Board T009 / Task 9 raw-result separation: active under `/tmp/vpa-task9-raw-result-separation-foundation-20260802.md` — user correction established that raw monolithic adequacy must remain constructible and provable if identity normalization does not exist. Audit found the incumbent `RelationJoinResult` itself owns normalization receipts and the raw join imports normalization-bearing splice machinery, so the result ownership must be rebuilt before the paused region atlas can establish adequacy.

Board T009 / Task 9 raw-splice separation: complete at `a415bef` — `Subgraph/SpliceRaw.lean` is the sole raw splice authority and has a normalization-free 23-module import closure; `Subgraph/Splice.lean` is only the downstream normalized result/application owner. Focused builds, formal size, exact declaration preservation, dependency scans, and fresh review pass with no findings. Next prerequisite: extract generic dense node/wire erasure from `IdentityNormalizationCore` before rebuilding the raw relation-join result.

Board T009 / Task 9 dense-erasure separation: complete at `ad5c3ef` — the generic dense node/wire deletion block moved unchanged from `IdentityNormalizationCore` to `ConcreteDiagram.DenseErasure`, whose only import is `Concrete.WellFormed`. All 446 generic qualified uses across 24 consumers were migrated; no stale old-owner reference, alias, duplicate, or normalization dependency remains. Focused/aggregate builds, size/diff/closure scans, and fresh review pass with no findings. Next: rebuild the relation-join constructor/result so its raw terminal output is independently constructible and the normalized public result is a distinct downstream owner.

Board T009 / Task 9 raw-result ownership: complete through `a764efe` and packaging correction `8f6de97` — raw splice construction, factorization insertion, relation-join result/checker, trace semantics, and terminal semantics now terminate at raw `plainFinal`; normalized splice/join behavior is a distinct downstream owner. Clean committed-snapshot builds passed for the raw atlas, monolithic specification, compiler, and compiler soundness. The large fixture module was externally killed twice with exit 137 at roughly 23.4 GiB RSS and no Lean diagnostic; this is recorded as an environment limit rather than source evidence.

Board T009 / Task 9 raw adequacy independence: complete at `7ee6b4e` with clean-tree import correction `0203037` — `AcceptedMonolithicRelationJoin` owns raw checker acceptance and `joinRelation = .ok`; compiler redundancy and soundness consume only the raw join/sever carriers and target join `plainFinal` or the raw sever target. The public normalized join is downstream. Neutral orientation/direction, intrinsic equality, and vacuity owners remove incidental structural-normalization imports, and unused normalized structural program constructors were deleted. Fresh exact-commit review found the raw monolithic/program/compiler/soundness closures contain 45/113/128/129 modules respectively and zero identity-normalization, normalized-result, or public-wrapper dependencies. Raw-only Lake passed 130/130 jobs, combined raw/public Lake passed 154/154, theorem signatures are raw, and their axiom sets contain only `propext`, `Classical.choice`, and `Quot.sound`. The preserved raw-region/atlas work remains active.

Board T010 / plan Task 9 GREEN checkpoint: blocked on T011 — constructive join/sever source and the focused 151-job compiler, soundness, and fixture gate pass; `formal:size` and diff hygiene pass. Commit `9026244` migrated the three stale normalization-semantics proofs to the existing origin-erased elaboration equations, and those modules now elaborate. Aggregate Lake then spent more than 20 CPU-minutes at 100% CPU in Task 10 `DerivedFixtures.lean` whole-compiler `native_decide` evaluation with stable memory and no diagnostic. No constructive-isomorphism theorem is missing. Activated T011 to replace the unbounded fixture validation and complete the remaining Task 10 interfaces before rerunning T010 GREEN.

Board T010 / plan Task 9: complete through constructive join `52fb5cd`, derived sever `573b4aa`, and terminal collision repair `95dd95a` — the exact missing endpoint obligation was source-side leaf factorization when a raw identity-request prefix retained an indistinguishable identity leaf. The private paired abstraction now treats acted applications and retained same-kind leaves uniformly and proves the corresponding denotation law; this is consumed by the terminal leaf checker used by the constructive compiler. Join constructs the raw `ConcreteIso` directly, sever reverses it through ordinary reconstruction, both searches and `redundancyMismatch` remain deleted, and the adequacy path has no normalization edge. Commit `e4044e6` reduced the queued Task 10 nullary fixture from an unnecessary parallel pair to the required single nullary item while retaining both orientations; focused DerivedFixtures completes in 24 seconds. Full `lake build` passes 190/190 jobs, focused compiler/soundness/fixtures pass 151/151, `formal:size` and diff hygiene pass. Activated T011 / plan Task 10.

Board T011 / corrected plan Task 10: complete — the active 34-step TypeScript authority resolved a stale design contradiction exposed by `/tmp/PerSiteDerivationProbe.lean`: forward nonuniform extension and uniform extension succeed at even scope, sever isolates the differing endpoint, but the required ordinary merge correctly rejects with `joinRequiresNegative`. The false per-site derivability interface and acceptance claim were removed instead of adding a thirty-fifth primitive or weakening generic join. Direct per-site `argExtend` remains independently gated, with the Task 7 canonical uniform/differing fixtures authoritative. Insertion now exposes exact ordered raw boundary transport through its constructive `ConcreteIso`; arbitrary and nullary programs execute in both orientations; the folded named-item/body law is pinned for Task 11; backward erasure still cites `negative_splice_sound` directly. Focused derivation/argument fixtures pass 157/157 in 27 seconds, full Lake passes 190/190, `formal:size`, placeholder/obsolete-claim scans, and diff hygiene pass. Activated T012 / plan Task 11. Foundation: `/tmp/vpa-task10-derivability-foundation-20260803-r4.md`.
